# 设计模式

软件开发中，前人对软件系统的设计和开发总结了一些原则和模式，不管使用什么语言来开发，这些设计模式都对软件设计工作中提供指导意义。

## 24大设计模式和7大原则

### 软件开发原则

[7大原则](../开发理论/开发理论.md)

### 创建型

用于创建对象的模式，对类的实例化进行抽象。

5种模式

* **单例模式 Singleton**
* **工厂方法模式 Factory Method**
* 抽象工厂模式 Abstract Factory
* **建造者模式 Builder**
* 原型模式 Prototype

### 结构型

处理类或者对象的组合，有两种方式，一种是继承机制来组合接口，一种是组合对象来实现新功能。

7种模式

* 适配器模式 Adapter
* 桥梁模式 Bridge
* **装饰器模式 Decorator**
* 门面模式 Facade
* 合成模式 Composite
* 享元模式 Flyweight
* **代理模式 Proxy**

### 行为型

在不同对象之间划分责任和算法，有两种方式，一种是使用继承关系来分配行为，一种是使用对象聚合的方式来分配行为。

11种模式

* **模板方法模式 Template Method**
* 命令模式 Command
* 迭代器模式 Iterator
* **观察者模式 Observer**
* 调停者模式 Mediator
* 状态模式 State
* **策略模式 Strategy**
* 责任链模式 Chain of Responsibility
* 访问者模式 Visitor
* 备忘录模式 Memento
* 解释器模式 Interpreter

### 关联关系

![img](assets/design_overview_3.png)

### 创建型 - 单例模式 Singleton 

使用一个私有构造函数、一个私有静态变量以及一个公有静态函数来实现。

私有构造函数保证了不能通过构造函数来创建对象实例，只能通过公有静态函数返回唯一的私有静态变量。

**懒汉式**

私有静态变量的唯一实例被延迟实例化，在没用到这个类的情况时，可以节省内存资源。

但这个实现在多线程环境下时**线程不安全**的

```java
public class Singleton {

    private static Singleton uniqueInstance;

    private Singleton() {
    }

    public static Singleton getUniqueInstance() {
        if (uniqueInstance == null) { // uniqueInstance 为 null时，多个线程能够同时进入，那么会有多个线程执行，导致多次实例化 uniqueInstance
            uniqueInstance = new Singleton();
        }
        return uniqueInstance;
    }
}
```

**饿汉式**

类加载时就实例化 uniqueInstance，不会产生线程安全问题，但是无法像延迟实例化那样节约资源。

```java
private static Singleton uniqueInstance = new Singleton();
```

**懒汉式-同步锁改进**

对 getUniqueInstance() 方法加锁，避免了多次实例化 uniqueInstance 的问题，但是其他线程的等待带来性能损耗。

```java
public static synchronized Singleton getUniqueInstance() {
    if (uniqueInstance == null) {
        uniqueInstance = new Singleton();
    }
     return uniqueInstance;
}
```

**懒汉式-双重校验锁**

第一个 `if`：只要uniqueInstance被实例化一次后，就可以直接使用了，线程没必要再按序等待。

`synchronized` 和第二个 `if`：类似上一个版本，为了避免多个线程获取锁后去实例化 uniqueInstance 的问题

`volatile` ：第一个线程进入时，实例化uniqueInstance的步骤如下，1 分配内存空间>> 2 初始化对象>> 3 将引用指向内存地址，JVM 的指令重排的特性可能使得 3 先于 2 执行，在第一个线程视角下是遵循遵守as-if-serial语义的（不管怎么重排序，单线程程序的执行结果都不能被改变），但可能导致**某个线程在同步代码块之外拿到和操作空的uniqueInstance**。使用 volatile 可以禁止 JVM 的指令重排，保证多线程环境下也能正常允许。

```java
public class Singleton {

    private volatile static Singleton uniqueInstance;

    private Singleton() {
    }

    public static Singleton getUniqueInstance() {
        if (uniqueInstance == null) {
            synchronized (Singleton.class) {
                if (uniqueInstance == null) {
                    uniqueInstance = new Singleton();
                }
            }
        }
        return uniqueInstance;
    }
}
```

